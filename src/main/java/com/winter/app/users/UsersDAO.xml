<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.winter.app.users.UsersDAO">
    
    <insert id="register" parameterType="UsersDTO">
    	INSERT INTO users (username, password, name, email, phone, birth)
		VALUES (#{username}, #{password}, #{name}, #{email}, #{phone}, #{birth})
    </insert>
    
    <select id="mypage" resultMap="mypageResult" parameterType="UsersDTO">
    	SELECT * 
        FROM users U
            LEFT JOIN
            profile P
            ON U.username = P.username
        WHERE U.username = #{username}
    </select>

    <resultMap type="UsersDTO" id="mypageResult">
        <id column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="birth" property="birth"/>
        <collection property="fileDTOs" javaType="java.util.List" ofType="UsersFileDTO">
            <id column="file_num" property="fileNum"/>
            <result column="file_name" property="fileName"/>
            <result column="file_origin" property="fileOrigin"/>
        </collection>
    </resultMap>
    
    <insert id="addProfile" parameterType="UsersFileDTO">
    	INSERT INTO profile (file_name, file_origin, username)
    	VALUES (#{fileName}, #{fileOrigin}, #{username})
    </insert>
    
    
    	
    </mapper>