<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.winter.app.users.UserDAO">
    
    <insert id="register" parameterType="UserDTO">
    	INSERT INTO users (username, password, name, email, phone, birth)
		VALUES (#{username}, #{password}, #{name}, #{email}, #{phone}, #{birth})
    </insert>
    
    <select id="mypage" resultMap="mypageResult" parameterType="UserDTO">
    	SELECT * 
        FROM users U
            LEFT JOIN
            profile P
            ON U.username = P.username
        WHERE U.username = #{username}
    </select>

    <resultMap type="UserDTO" id="mypageResult">
        <id column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="birth" property="birth"/>
        <collection property="fileDTOs" javaType="java.util.List" ofType="UserFileDTO">
            <id column="file_num" property="fileNum"/>
            <result column="file_name" property="fileName"/>
            <result column="file_origin" property="fileOrigin"/>
        </collection>
    </resultMap>
    
    <insert id="addProfile" parameterType="UserFileDTO">
    	INSERT INTO profile (file_name, file_origin, username)
    	VALUES (#{fileName}, #{fileOrigin}, #{username})
    </insert>
    
    <update id="update" parameterType="UserDTO">
        UPDATE users
        <set>
        	<if test="password != null and password != ''">
        		password = #{password},
        	</if>
        	name=#{name},
        	<if test="email != null">
        		email=#{email},
        	</if>
        	<if test="phone != null">
        		phone = #{phone},
        	</if>
        	<if test="birth != null">
        		birth=#{birth}
        	</if>
        </set>
       	WHERE username = #{username} 
    </update>

    <delete id="deleteProfile" parameterType="UserFileDTO">
        DELETE FROM profile
        WHERE file_num = #{fileNum}
    </delete>

    <update id="updateProfile" parameterType="UserFileDTO">
        UPDATE profile
        SET file_name = #{fileName},
            file_origin = #{fileOrigin}
        WHERE username = #{username}
    </update>
    
    <update id="updatePassword" parameterType="UserDTO">
        UPDATE users
        SET password = #{password}
        WHERE username = #{username}
    </update>

	<select id="findByEmail" resultMap="mypageResult" parameterType="UserDTO">
		SELECT *
		FROM users
		WHERE email = #{email}
	</select>
    	
    </mapper>